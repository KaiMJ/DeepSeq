"""
ML based
    - Linear regression, Lasso regression (sklearn library should be fine)
    - SVM (sklearn as well)
    - PCA (lower dimension and apply linear regression or SVM)
    - random forest(n_estimators, criterion, max_depth, max_features,...)
    - XGBoost(n_estimators, max_depth, learning_rate, …)
Forecasting models
    - ARIMA, SARIMA
    - Prophet
Statistical Models
    - Kalman / particle filter (OHLC only)
Deep learning models (input_dim, ouptut_dim, *args)
    - DNN (n_layers, hidden_dim)
    - 1D-CNN (n_kernels, n_layers)
    - RNN (encoder_dim, hidden_dim, n_layers)
    - LSTM (encoder_dim, hidden_dim, n_layers)
    - Transformers (n_heads, hidden_dim, n_layers)
"""
